# ─── Environment ───
NODE_ENV=development

# ─── OpenAI ───
# The app stores the user's API key locally (encrypted).
# No key here is needed.

# ─── Backend (optional — for auth & credits) ───
MEETVORA_API_URL=http://localhost:5001
